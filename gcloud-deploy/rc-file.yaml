apiVersion: v1
kind: ReplicationController
metadata:
  name: szeged-jupyterhub-rc
spec:
  replicas: 1
  selector:
    name: szeged-jupyterhub
    version: v1
  template:
    metadata:
      labels:
        name: szeged-jupyterhub
        version: v1
    spec:
      containers:
        - name: jhub
          image: eu.gcr.io/jupyterhub-szeged/final-image
          ports:
            - containerPort: 8000
    